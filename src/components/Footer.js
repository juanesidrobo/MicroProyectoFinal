
import React from 'react';

const Footer = () => {
  return <footer style={styles.footer} />;
};

const styles = {
  footer: {
    position: 'fixed',
    bottom: 0,
    left: 0,
    width: '100%',
    height: '40px',
    backgroundColor: '#c78c6f',
  },
};

export default Footer;
